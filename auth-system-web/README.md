# Auth System Web

React frontend for the authentication system.

## Features

- Login form
- Registration form
- Connects to Laravel API
- Handles token authentication

## Setup

```bash
cd auth-system-web
npm install
npm start
```

Frontend will run on: `http://localhost:3000`

## Notes

- Make sure the API backend is running
- Configure `.env` if needed for API base URL
